{
  "name": "Spring Boot 3.5.0 - Java 21",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [ "--privileged" ],
  "remoteUser": "rootless",
  "postCreateCommand": "ssh -V || echo 'SSH not found' && mvnd -U -Dquickly -DskipTests clean install && dockerd-entrypoint.sh — experimental &"
}
